<h1>Listing feeds</h1>

<table>
  <tr>
    <th>Feed url</th>
    <th>Site url</th>
    <th>Title</th>
    <th>Feedlist</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @feeds.each do |feed| %>
  <tr>
    <td><%= feed.feed_url %></td>
    <td><%= feed.site_url %></td>
    <td><%= feed.title %></td>
    <td><%= feed.feedlist_id %></td>
    <td><%= link_to 'Show', feed %></td>
    <td><%= link_to 'Edit', edit_feed_path(feed) %></td>
    <td><%= link_to 'Destroy', feed, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Feed', new_feed_path %>
