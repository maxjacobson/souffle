<ul>
  <% @folders.each do |folder| %>
    <li><%= folder[:title] %>
      <ul>
        <% folder[:feeds].each do |feed| %>
          <li><a href="<%= feed[:url] %>"><%= feed[:title] %></a></li>
        <% end %>
      </ul>
    </li>  
  <% end %>
</ul>